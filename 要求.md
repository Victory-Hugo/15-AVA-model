## GUI 需求概述
- 目标：将原 Bash 脚本 `F:\OneDrive\文档（科研）\脚本\Download\15-AVA-model\Windows\pipe\1-pipe.py` 
- 界面要求：全部英文，无中文；配置分 Tab 展示。
- 参数分区：
  - Main：PY scripts folder、KEY_COLUMN、UNIQUE_KEYS、TIME_COLUMN、IN_TIME_CSV path、IN_AMOVA_CSV path、IN_UNIQ_CSV path、Output folder (OUT)。
  - Time Depth：--ancient_threshold、--ratio_quantile、--kernel_sigma、--time_depth_sigma_log10。
  - Time Distribution：--gmm_max_components、--gmm_min_samples、--random_state、--skew_method。
  - AMOVA：--variation_type、--variation_value。
  - Unique：--keyword（可填 "UNIQUE_KEYS" 以复用 UNIQUE_KEYS）。
  - Score Weights：--w-time-depth、--w-time-structure、--w-diversity、--w-unique。

